#include "controller.h"
//this is completely wrong please change
int main(int argc, char* argv[]) {
    bool graphics = false;

    Controller controller(graphics);
    controller.run();
    return 0;
}